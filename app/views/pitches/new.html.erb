<%= form_for @pitch, url: timeslot_pitches_path(params[:timeslot_id]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.label :image_url %>
  <%= f.text_field :image_url %>
  <%= f.submit "Submit Pitch!" %>
<% end %>
